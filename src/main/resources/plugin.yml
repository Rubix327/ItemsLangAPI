name: ${project.name}
version: ${project.version}
main: ${main.class}
author: ${author}
api-version: 1.13
commands:
  ila:
    description: the main command
    usage: /<command> <test | reload>
    default: op